import {
  Section,
  Title
} from 'rbx'
import Layout from '../components/Layout'

const Timer = () => (
  <Layout>
    <Section>
      <Title as="h2">Timer</Title>
    </Section>
  </Layout>
)

export default Timer
